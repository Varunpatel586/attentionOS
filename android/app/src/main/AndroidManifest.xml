<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.attentionos">

    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- Required for foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    
    <!-- Required for notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- Required for usage stats (special permission, requires user to grant in Settings) -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />
    
    <!-- Required for floating overlay bubble -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>

      <!-- Foreground Service for tracking -->
      <service
          android:name=".service.TrackingForegroundService"
          android:enabled="true"
          android:exported="false"
          android:foregroundServiceType="dataSync" />

      <!-- Accessibility Service for scroll detection -->
      <service
          android:name=".accessibility.ScrollDetectionAccessibilityService"
          android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
          android:enabled="true"
          android:exported="false">
          <intent-filter>
              <action android:name="android.accessibilityservice.AccessibilityService" />
          </intent-filter>
          <meta-data
              android:name="android.accessibilityservice"
              android:resource="@xml/accessibility_service_config" />
      </service>

      <!-- Widget Update Service -->
      <service
          android:name=".service.WidgetUpdateService"
          android:enabled="true"
          android:exported="false" />

      <!-- Notification action receiver to stop tracking -->
      <receiver
          android:name=".receiver.StopTrackingReceiver"
          android:enabled="true"
          android:exported="false" />

      <!-- Focus Scroll Widget -->
      <receiver android:name=".widgets.FocusScrollWidgetProvider"
          android:enabled="true"
          android:exported="true">
          <intent-filter>
              <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
              <action android:name="com.attentionos.UPDATE_FOCUS_SCROLL_WIDGET" />
          </intent-filter>
          <meta-data
              android:name="android.appwidget.provider"
              android:resource="@xml/widget_focus_scroll_info" />
      </receiver>

      <!-- Tasks Widget -->
      <receiver android:name=".widgets.TasksWidgetProvider"
          android:enabled="true"
          android:exported="true">
          <intent-filter>
              <action android:name="com.attentionos.UPDATE_TASKS_WIDGET" />
              <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
          </intent-filter>
          <meta-data
              android:name="android.appwidget.provider"
              android:resource="@xml/widget_tasks_info" />
      </receiver>

      <!-- Timer Widget -->
      <receiver android:name=".widgets.TimerWidgetProvider"
          android:enabled="true"
          android:exported="true">
          <intent-filter>
              <action android:name="com.attentionos.UPDATE_TIMER_WIDGET" />
              <action android:name="com.attentionos.TIMER_PAUSE" />
              <action android:name="com.attentionos.TIMER_RESUME" />
              <action android:name="com.attentionos.TIMER_RESET" />
              <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
          </intent-filter>
          <meta-data
              android:name="android.appwidget.provider"
              android:resource="@xml/widget_timer_info" />
      </receiver>

      <!-- Timer Control Receiver -->
      <receiver android:name=".receiver.TimerControlReceiver"
          android:enabled="true"
          android:exported="false">
          <intent-filter>
              <action android:name="com.attentionos.TIMER_PAUSE" />
              <action android:name="com.attentionos.TIMER_RESUME" />
              <action android:name="com.attentionos.TIMER_RESET" />
          </intent-filter>
      </receiver>

      <!-- Timer Event Receiver for React Native -->
      <receiver android:name=".receiver.TimerEventReceiver"
          android:enabled="true"
          android:exported="false">
          <intent-filter>
              <action android:name="com.attentionos.TIMER_EVENT" />
          </intent-filter>
      </receiver>

    </application>
</manifest>
